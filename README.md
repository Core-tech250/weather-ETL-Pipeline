# weather-ETL-Pipeline
A modular ETL pipeline that fetches real-time weather data from an API, transforms it into a structured schema, and loads it into a SQLite database with duplicate protection and loggin
